# VACASK 4-stage diode multiplier setup

`runme.sim` (symlink) → upstream's `VACASK/benchmark/mul/vacask/runme.sim`.
`prepare.py` (symlink) → upstream's pre-build hook.

Fixed topology — no per-N generator.
