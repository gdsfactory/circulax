# VACASK RC low-pass setup

`runme.sim` (symlink) → upstream's `VACASK/benchmark/rc/vacask/runme.sim`.
`prepare.py` (symlink) → upstream's pre-build hook.

This benchmark is fixed-topology — there is no per-N generator.
